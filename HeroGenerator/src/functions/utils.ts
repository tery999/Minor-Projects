export function diceRollFunction (topNumber:number):number {
    const diceRoll = Math.floor(Math.random() * topNumber) + 1;
    return diceRoll;
}

//friendship generator would become too big, exporting logic here
export function friendsCombFunc(optionalSurrounding = ""):string {
    let personCharacter = "";



    return personCharacter;
}